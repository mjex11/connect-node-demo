/**
 * Validates a trailer for the gRPC and the gRPC-web protocol.
 * Throws a ConnectError if the trailer contains an error status.
 */
export declare function validateTrailer(trailer: Headers): void;
